# Workflow Inventory

One document per operation/workflow, using `_TEMPLATE_workflow.md`. Copy to
`<NN>_<WorkflowName>.md` and fill every field. Document the legacy behaviour exactly as observed.

## Workflow index

| # | Workflow | Start Screen | Trigger | Status | Doc |
|---|----------|--------------|---------|--------|-----|
| 01 | | | | Empty | |
