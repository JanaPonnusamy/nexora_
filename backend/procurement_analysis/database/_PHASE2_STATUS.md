# Phase 2 — Status: BLOCKED (database unavailable)

> Recorded 2026-06-28. Read-only diagnostics only; **no database modifications were made.**

## Blocker

The Phase 2 deliverables are generated by `../queries/run_phase2_extract.py` (read-only metadata
extraction). The script and connection were verified, but the target database **cannot be opened**:

| Database | State | Notes |
|----------|-------|-------|
| **OrderNMC** (LPIE target) | **`RECOVERY_PENDING`** | Cannot be opened (SQL error 4060). Files present & ONLINE. |
| `Order` | ONLINE | 34 tables / 7 procs — **not** the procurement schema (no `customer_demands`, `order_cycles`, `nx_sp_*`). |
| `NEXORA_PLATFORM` | ONLINE | 50 tables / 103 procs — central platform DB, **not** legacy procurement. |

`OrderNMC` is the **only** database on the instance that contains the legacy procurement objects
(verified earlier this session: `nx_sp_BuildVirtualProductList`, `nx_sp_GetProductProcurementMetrics`,
`customer_demands`, `order_cycles`, `order_virtual_items`, `procurement_rows`, …). No equivalent source
exists on the instance.

## Diagnostics (read-only)

- Instance: `192.168.10.73` — Microsoft SQL Server **2014** (12.0.2000.8), login `sa` works (connects to `master`).
- `sys.databases`: `OrderNMC` = `RECOVERY_PENDING`, `MULTI_USER`, `SIMPLE`.
- `sys.master_files` for OrderNMC:
  - ROWS `OrderNMC` → `D:\Data\OrderNMC.mdf` — file state **ONLINE**
  - LOG `OrderNMC_log` → `D:\Data\OrderNMC_log.ldf` — file state **ONLINE**

The data/log files exist and report ONLINE at the file level; the database itself failed to complete
recovery (typical after an unclean instance shutdown on SQL Server 2014).

## Remediation (to be performed by the DBA / database owner — not by this analysis)

`RECOVERY_PENDING` usually clears with a standard, non-schema recovery action. Suggested checks, least
intrusive first:

1. **Inspect the SQL Server error log** for the reason recovery did not start (look for the `OrderNMC`
   startup entries).
2. Bring the database online (runs crash recovery):
   ```sql
   ALTER DATABASE OrderNMC SET ONLINE;
   ```
   *(This is a database **state** change — intentionally left for the owner to run, to honour the
   "do not modify" rule of this phase.)*
3. If step 2 reports a torn/locked file, confirm no other process holds `OrderNMC.mdf`/`.ldf`, then retry.
4. Alternatively, provide one of the Phase 2 evidence options against which the script can run unchanged:
   a restored **`.bak`**, attached **MDF+LDF** (as a differently-named DB), or a **read-only login** to a
   recovered copy. Override the target via env vars `ORDERNMC_SERVER` / `ORDERNMC_DB` / `ORDERNMC_UID` /
   `ORDERNMC_PWD`.

## How to complete Phase 2 once OrderNMC is ONLINE

```bash
python backend/procurement_analysis/queries/run_phase2_extract.py
```

This writes all eight deliverables into `backend/procurement_analysis/database/`:
`database_inventory.md`, `key_relationships.md`, `database_objects.md`, `dependency_analysis.md`,
`procurement_classification.md`, `data_dictionary.md`, `stored_procedure_catalogue.md`, `evidence_log.md`.

The script is **read-only** (SELECT / catalog views / `sys.sql_modules` / `sys.sql_expression_dependencies`
only) and makes **no** data or schema changes.
