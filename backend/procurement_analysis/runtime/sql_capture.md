# Runtime SQL Capture (Legacy)

> Record the **actual SQL executed at runtime** per operation (from code inspection and/or SQL Profiler /
> Extended Events traces). This reveals real execution order, parameters and tables touched. Read-only
> observation; capture **must not** run against production without authorization. Status: `Empty`.

## Capture method
| Method used | Tool | Scope | Date | Captured by | Evidence |
|-------------|------|-------|------|-------------|----------|
| | code inspection / Profiler / XEvents | | | | |

## Per-operation capture

### Operation: <name>
| # | Executed Query / SP | Parameters | Updated Tables | Returned Data | Order | Evidence |
|---|---------------------|------------|----------------|---------------|-------|----------|
| 1 | | | | | | |

> Repeat the block per operation (Load Order Data, Build Order, Assign Supplier, Export, GRN, etc.).
> Quote the query text exactly; redact connection strings/credentials.
