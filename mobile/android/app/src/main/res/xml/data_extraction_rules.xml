<?xml version="1.0" encoding="utf-8"?>
<!--
  The local database can contain invoice data and queued business mutations.
  It must not move into cloud backup or device-to-device transfer outside the
  authenticated app. `allowBackup=false` is the primary guard; these explicit
  Android 12+ rules make the intended treatment auditable as well.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
