"""NDF builders: pure model-to-text rendering.

Builders contain no side effects — they take typed models and return strings. All
persistence is performed by services via the filesystem provider.
"""
