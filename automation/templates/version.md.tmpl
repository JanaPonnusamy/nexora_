# NEXORA Platform — Version

> This file is generated and maintained by the NEXORA Development Framework (NDF).
> The fenced `toml` block below is the machine-readable source of truth. Edit via
> `python -m automation.version bump <major|minor|patch|build>`.

**Current Version:** `{{semver}}`  ·  **Build:** `{{build_number}}`  ·  **Release:** {{release_name}}

```toml
[version]
major = {{major}}
minor = {{minor}}
patch = {{patch}}
build_number = {{build_number}}
build_date = "{{build_date}}"
release_name = "{{release_name}}"
developer = "{{developer}}"
git_commit = "{{git_commit}}"
branch = "{{branch}}"
```

| Field | Value |
|-------|-------|
| Major | {{major}} |
| Minor | {{minor}} |
| Patch | {{patch}} |
| Build Number | {{build_number}} |
| Build Date | {{build_date}} |
| Release Name | {{release_name}} |
| Developer | {{developer}} |
| Git Commit | {{git_commit}} |
| Branch | {{branch}} |
