"""NDF command handlers (the CLI surface).

Each module exposes a ``main(argv)`` entry point. The thin top-level shims
(``automation/version.py`` etc.) and the unified dispatcher (``automation/__main__.py``)
delegate here.
"""
