# Nexora Store Agent Setup Wizard - build + runtime dependencies
requests>=2.31
cryptography>=41.0
pywin32>=306
pyodbc>=5.0
pyinstaller>=6.0
# tkinter ships with the standard CPython Windows installer
